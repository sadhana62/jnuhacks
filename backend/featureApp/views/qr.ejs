<!DOCTYPE html>
<html>
<head>
  <title>Students QR Codes</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      background: #f8f9fa;
      margin: 20px;
    }
    h1 {
      text-align: center;
      margin-bottom: 30px;
      color: #333;
    }
    .grid-container {
      display: grid;
      grid-template-columns: repeat(4, 1fr);
      gap: 20px;
      justify-items: center;
    }
    .student-card {
      background: #fff;
      border: 1px solid #ddd;
      border-radius: 12px;
      padding: 15px;
      text-align: center;
      box-shadow: 0 2px 6px rgba(0,0,0,0.1);
      transition: transform 0.2s;
    }
    .student-card:hover {
      transform: scale(1.05);
    }
    .qr img {
      width: 150px;
      height: 150px;
      margin-bottom: 10px;
    }
    .name {
      font-weight: bold;
      font-size: 18px;
      margin: 5px 0;
      color: #222;
    }
    .id {
      font-size: 15px;
      color: #666;
      margin-bottom: 10px;
    }
    .download-btn {
      display: inline-block;
      padding: 8px 14px;
      background: #007bff;
      color: white;
      border-radius: 6px;
      text-decoration: none;
      font-size: 14px;
      transition: background 0.3s;
    }
    .download-btn:hover {
      background: #0056b3;
    }
    @media print {
      body {
        background: white;
      }
      .download-btn {
        display: none; /* Hide buttons when printing */
      }
      .student-card {
        box-shadow: none;
        border: 1px solid #ccc;
      }
    }
  </style>
</head>
<body>
  <h1>All Students QR Codes</h1>

  <div class="grid-container">
    <% students.forEach(student => { %>
      <div class="student-card">
        <div class="qr">
          <img src="<%= student.qr %>" alt="QR Code">
        </div>
        <div class="name"><%= student.name %></div>
        <div class="id">ID: <%= student.id %></div>
        <a class="download-btn" href="<%= student.qr %>" download="student_<%= student.id %>.png">Download</a>
      </div>
    <% }) %>
  </div>
</body>
</html>
